[phases.setup]
aptPkgs = ["libpango-1.0-0", "libpangocairo-1.0-0", "libpangoft2-1.0-0", "libharfbuzz0b", "libgdk-pixbuf2.0-0", "libcairo2", "libffi-dev", "shared-mime-info", "fonts-dejavu-core", "fonts-liberation"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
